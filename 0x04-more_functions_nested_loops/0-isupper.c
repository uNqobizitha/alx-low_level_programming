#include "main.h"
/**
 * _isupper -prog checks for uppercase character
 * @c: Variable text
 * Return: Always 0.
 */
int _isupper(int c)
{
if (c >= 'A' && c <= '2')
{
return (1);
}
else
{	
return (0);
}
}
